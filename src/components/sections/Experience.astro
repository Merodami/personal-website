---
import Section from '../common/Section.astro';
import Card from '../common/Card.astro';
import Badge from '../common/Badge.astro';
import { experiences } from '../../data/experience';
---

<Section id="experience">
  <div class="grid md:grid-cols-2 gap-12 items-start">
    <div>
      <h2 class="text-3xl md:text-4xl font-bold text-white mb-4">Career & Experience</h2>
      <p class="text-gray-300 text-lg">
        I've had the privilege of working with talented teams and contributing to exciting projects
        throughout my career. Here's a glimpse of my professional journey.
      </p>
    </div>

    <div class="space-y-6">
      {
        experiences.map((exp) => (
          <Card hover={false} class="relative">
            <div class="flex items-start gap-4">
              <div class="flex-shrink-0 w-2 h-2 bg-purple-400 rounded-full mt-2" />
              <div class="flex-grow">
                <div class="flex flex-wrap items-center gap-2 mb-2">
                  <h3 class="text-xl font-semibold text-white">{exp.position}</h3>
                  <span class="text-gray-400">at</span>
                  <span class="text-purple-400 font-medium">{exp.company}</span>
                </div>

                <div class="text-sm text-gray-400 mb-3">
                  {exp.startDate} - {exp.endDate}
                </div>

                <p class="text-gray-300 mb-4">{exp.description}</p>

                <div class="flex flex-wrap gap-2">
                  {exp.technologies.map((tech) => (
                    <Badge text={tech} variant="primary" />
                  ))}
                </div>
              </div>
            </div>
          </Card>
        ))
      }
    </div>
  </div>
</Section>
